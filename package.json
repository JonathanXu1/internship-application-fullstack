{
  "private": true,
  "name": "Jonathan Xu's submission",
  "version": "1.0.0",
  "description": "My submission for Cloudflare's full stack internship position",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Jonathan Xu <j462xu@uwaterloo.ca>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "node-fetch": "^2.6.0"
  }
}